p.date Date
script
  var today = new Date().toLocaleTimeString('en-US', {
    weekday: 'long',
    month: 'long',
    day: '2-digit',
    year: 'numeric',
    hour: '2-digit',
    minute: '2-digit'
  });
  $('.date').html(today);