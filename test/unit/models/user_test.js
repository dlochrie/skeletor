var should = require('should');

/**
 * TODO:
 * These tests need to be written, but the Models need to be fixed so that
 * a model can be defined and used here to test each Unit in isolation.
 */
describe('User Model', function() {
  it('should get one User', function(done) {
    // TBI.
    done();
  });

  it('should find all Users WITHOUT arguments', function(done) {
    // TBI.
    done();
  });

  it('should find all Users WITH arguments', function(done) {
    // TBI.
    done();
  });

  it('should find the latest Users WITHOUT arguments', function(done) {
    // TBI.
    done();
  });

  it('should find the latest Users WITH arguments', function(done) {
    // TBI.
    done();
  });

  it('should create a new User', function(done) {
    // TBI.
    done();
  });

  it('should edit an existing User', function(done) {
    // TBI.
    done();
  });

  it('should delete an existing User', function(done) {
    // TBI.
    done();
  });
});
